import apiBuilder from 'data-grid/utils/api-builder';
import { module, test } from 'qunit';

module('Unit | Utility | api-builder', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = apiBuilder();
    assert.ok(result);
  });
});
